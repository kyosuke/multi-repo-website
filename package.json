{
  "private": true,
  "dependencies": {
    "@11ty/eleventy": "^0.11.1",
    "svelte-app": "github:kyosuke/multi-repo-website-private-spa"
  },
  "scripts": {
    "start": "npm run build -- --serve",
    "build": "eleventy --input=./src",
    "setup": "cp -R ./node_modules/svelte-app/public/build ./_site/spa/"
  },
  "bundleDependencies": [
    "svelte-app"
  ]
}
